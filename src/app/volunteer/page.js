import { VolunteerPage } from "@/sections/pages/Volunteer";

export const metadata = {
  title: "Volunteer — FreedomPulse",
  description:
    "Join the FreedomPulse campaign. Knock doors, run phone banks, host events — match your time to a captain in your precinct.",
};

export default function Page() {
  return <VolunteerPage />;
}
