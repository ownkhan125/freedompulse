import { DonatePage } from "@/sections/pages/Donate";

export const metadata = {
  title: "Donate — FreedomPulse",
  description:
    "Chip in to keep the FreedomPulse field operation running. No corporate PACs — every dollar from neighbors.",
};

export default function Page() {
  return <DonatePage />;
}
