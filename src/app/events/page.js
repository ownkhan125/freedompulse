import { EventsPage } from "@/sections/pages/Events";

export const metadata = {
  title: "Events — FreedomPulse",
  description:
    "Town halls, ride-alongs, coffee mornings, and listening sessions across the district.",
};

export default function Page() {
  return <EventsPage />;
}
