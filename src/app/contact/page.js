import { ContactPage } from "@/sections/pages/Contact";

export const metadata = {
  title: "Contact — FreedomPulse",
  description:
    "Reach the FreedomPulse campaign — questions, press, partnerships, or just to say hello.",
};

export default function Page() {
  return <ContactPage />;
}
